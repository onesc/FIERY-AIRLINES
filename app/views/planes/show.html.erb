<div class="panel panel-default">
  <div class="panel-heading"><p id="notice"><%= notice %></p></div>
  <div class="panel-body">
    <p>
      <strong>Plane Name:</strong>
      <%= @plane.name %>
    </p>

    <p>
      <strong>Rows</strong>
      <%= @plane.rows %>
    </p>

    <p>
      <strong>Columns</strong>
      <%= @plane.columns %>
    </p>

<%= link_to 'Edit', edit_plane_path(@plane), :class => 'button' %> |
<%= link_to 'Back', planes_path, :class => 'button' %>
</div>
</div>
