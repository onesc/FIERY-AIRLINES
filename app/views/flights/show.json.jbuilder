json.extract! @flight, :id, :name, :origin, :destination, :departure, :plane_id, :created_at, :updated_at
# t.string :name
# t.string :origin
# t.string :destination
# t.datetime :departure
# t.integer :plane_id
